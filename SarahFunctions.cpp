#include "SarahFunctions.h"
#include "structures.h"
#include <list>
#include <iostream>
#include <iterator>
#include <string>

int logIn(list<q>)
{
  string queryID;
  string queryPass;
  
  for (list<q>::iterator it = q.begin(); it != q.end(); ++it)
	{
		if(it->ID == 
	}
}
